<!DOCTYPE html>
<html>
	<head>
	<%= stylesheet_link_tag('application', :media => 'all') %>
	  <title>MovieDb</title>
	</head>
	<body>
		<!-- before yield -->
		<%= yield %>
		<!-- after yield -->
		<%= javascript_include_tag :defaults %>
	</body>
</html>
